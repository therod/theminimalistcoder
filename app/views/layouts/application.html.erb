<!DOCTYPE html>
<html lang="en">
  <head>
    <title>The Minimalist Coder</title>
    <% if @post %>
      <meta property="og:image" content="<%= image_url asset_path(@post.image) %>"/>
      <meta property="og:title" content="<%= @post.title %>" />
      <link rel="image_src" href="<%= image_url asset_path(@post.image) %>"/>
    <% end %>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <script type="text/javascript" src="//use.typekit.net/fjr3bxd.js"></script>
    <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
    <%= csrf_meta_tags %>
    <%= render 'shared/analytics' %>
  </head>

  <body>
    <div class="page-wrapper">

      <%= render 'layouts/header' %>

      <div class="content">
        <%= yield %>
      </div>

      <footer>
        <span class="copyright df">
          &copy; 2015 theminimalistcoder
        </span>
        <%# <div class="social_navigation"> %>
        <%#   <%= render 'layouts/social' %1> %>
        <%# </div> %>
      </footer>
    </div>

  </body>
</html>
